---
import pixel from "~/const/pixel";
---

{
  pixel.map((el) => {
    return (
      <div class={"qy"}>
        {el.map((value) => (
          <div class={`qx qx${value}`} />
        ))}
      </div>
    );
  })
}
