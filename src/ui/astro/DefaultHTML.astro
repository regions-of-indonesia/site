---
import "~/styles/globals.css";

import HTML from "./HTML.astro";
import PrimaryMetaTags from "./PrimaryMetaTags.astro";

type Props = {
  title?: string;
  description?: string;
  url?: string;
  image?: string;
};

const {
  title = "regions of indonesia",
  description = "regions of indonesia",
  url = "https://regions-of-indonesia.netlify.app",
  image = "https://regions-of-indonesia.netlify.app/image.png",
} = Astro.props;
---

<HTML data-theme="dark">
  <Fragment slot="head">
    <PrimaryMetaTags title={title} description={description} url={url} image={image} />

    <slot name="head" />
  </Fragment>
  <Fragment slot="body">
    <slot />
  </Fragment>
</HTML>
